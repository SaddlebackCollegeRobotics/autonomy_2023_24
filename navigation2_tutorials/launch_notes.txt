# Run the logged waypoint follower.
ros2 run nav2_gps_waypoint_follower_demo logged_waypoint_follower waypoint_1.yaml

# Run mapviz visualizer and point-to-click waypoint following.
ros2 launch nav2_gps_waypoint_follower_demo mapviz.launch.py 

# TODO - Either convert the wg84 to map using the interactive waypoint follower script code,
or have mapviz convert it automatically by setting the clicked point frame to map???
Change name of topic to /goal_pose which must be a geometry_msgs/PoseStamped.
The /clicked_point topic is a geometry_msgs/PointStamped.
```